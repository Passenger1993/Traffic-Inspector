name: traffic_seg
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - torch==2.0.1
      - torchvision==0.15.2
      - albumentations==1.3.1
      - opencv-python==4.8.1.78
      - matplotlib==3.7.2
      - numpy==1.24.3
      - pytest==7.4.0
      - pyyaml==6.0
      - tqdm==4.65.0
      - tensorboard==2.13.0
      - kagglehub==0.1.5          # для загрузки датасета с Kaggle (опционально)
      - fiftyone==0.23.2          # если нужен для просмотра данных
      - jupyter==1.0.0            # для работы с блокнотами
      - seaborn==0.12.2           # для расширенной визуализации
      - scikit-learn==1.3.0       # для дополнительных метрик